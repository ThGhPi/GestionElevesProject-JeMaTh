server {
    listen 80;
    server_name _;

    # Serve uploaded files
    location /uploads/ {
        alias /usr/share/nginx/html/uploads/;
        autoindex off;
        access_log off;
    }

    # (Optional) deny hidden files like .git or .env
    location ~ /\. {
        deny all;
    }
}